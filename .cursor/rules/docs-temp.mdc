---
description: 临时文档不提交规则
globs: docs/**/*.md
alwaysApply: false
---

# 临时文档与 docs/_temp/

## 规则

- **`docs/_temp/`**：专门存放临时性、草稿、问题分析等**不提交到 git** 的 md 文件
- 该目录已在 `.gitignore` 中，**永远不要**将 `docs/_temp/` 下的文件加入 git
- 后续新增的临时 md 文件（如问题分析、Cline 参考、任务拆解草稿等）应放入 `docs/_temp/`

## 示例

应放入 `docs/_temp/` 的文件类型：
- 问题分析（如 `问题分析_xxx_关键代码未补全.md`、`问题分析_链路追问机制缺失.md`）
- 参考文档草稿（如 `Cline参考_上下文发现与MCP流程.md`）
- 其他仅本地参考、不需版本控制的文档

## 引用处理

若从 `docs/` 移入 `docs/_temp/` 的文件曾被其他文档引用，需同步更新或移除这些引用。
