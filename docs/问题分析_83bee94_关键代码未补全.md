# 问题分析：83bee94 任务关键代码未补全

## 一、实际执行流程（来自 audit 日志）

| 步骤 | 工具 | 结果 | 耗时 |
|------|------|------|------|
| 1 | index.get_status | ✓ | 196ms |
| 2 | correlation.get_info | ✓ | 3277ms |
| 3 | code.search | ✓ | 16ms |
| 4 | code.read | ✓ | 2ms |
| 5 | analysis.synthesize | ✓ | 52476ms |

**结论**：code.read 已执行且成功，并非「未调用」。

---

## 二、根因定位（来自 diagnosis_summary.key_evidence_refs）

code.search 返回的 hits 顺序：

1. `knowledge-service/target/classes/.../CourseWorkflowMessageListener.class`（.class）
2. `knowledge-service/target/classes/.../OrderedWFBizCallbackConsumerListener.class`（.class）
3. `knowledge-service/src/main/java/.../CourseWorkflowMessageListener.java`（.java，line 65）

**当前逻辑**：`_extract_file_path_from_tool_results` 取 **hits[0]** 作为注入的 file_path。

**实际注入**：`CourseWorkflowMessageListener.class`（第一个命中）

**结果**：code.read 读取了 .class 二进制文件，`path.read_text()` 得到乱码，Synthesis 无法解析，报告「补充的证据并未提供具体代码」。

---

## 三、根因总结

| 问题 | 说明 |
|------|------|
| 根本原因 | 注入 file_path 时取 hits[0]，而 Zoekt 返回顺序中 .class 在 .java 之前 |
| 表现 | code.read 读取 .class 得到乱码，无法作为有效证据 |
| 正确做法 | 优先选择 .java/.py/.kt 等源码路径，而非 .class |

---

## 四、建议修复

在 `_extract_file_path_from_tool_results` 中：**遍历 hits，优先返回 .java/.py/.kt 等源码路径，若无再退回 hits[0]**。
