# RootSeeker v2.0.0 测试计划（细化版）

基于 `docs/v2.0.0_实施检查清单.md` 与 `docs/提示词计划_v2.0.0.md` 的验收标准编制。

---

## 一、测试范围总览

| 模块 | 测试文件 | 现有用例数 | 状态 | 优先级 |
|------|----------|------------|------|--------|
| MCP 网关 | test_mcp.py | 7 | ✅ 已覆盖 | P1/P2 |
| MCP API 集成 | test_mcp_api.py | 4 | ✅ 已覆盖 | P1 |
| AI 网关 | test_ai_gateway.py | 6 | ✅ 已覆盖 | P1/P2 |
| AI 驱动主流程 | test_orchestrator.py | 9 | ✅ 已覆盖 | P1/P2 |
| 脱敏与安全 | test_utils_redact.py | 8 | ✅ 已覆盖 | P2 |
| 配置热更新 | test_config_reload.py | 2 | ✅ 已覆盖 | P2/P3 |
| correlation_id | test_correlation_id.py | 3 | ✅ 已覆盖 | P1/P2 |
| 超时/重试 | test_timeout_retry.py | 1 | ⏳ 占位 | P2/P3 |
| 外部 MCP Server | test_external_mcp.py | 4 | ✅ 已覆盖 | P2 |
| npx 探测 | test_npx_probe.py | 2 | ✅ 已覆盖 | - |
| 向后兼容 | test_backward_compat.py | 4 | ✅ 已覆盖 | P1 |

---

## 二、MCP 网关与 API 测试

### 2.1 已覆盖（test_mcp.py）

| 用例 | 描述 | 状态 |
|------|------|------|
| test_register_and_list_tools | 工具注册与 list_tools 返回 | ✅ |
| test_call_tool_success | 正常调用返回正确结果 | ✅ |
| test_call_tool_not_found | 工具不存在返回 TOOL_NOT_FOUND | ✅ |
| test_call_tool_invalid_params | 缺参返回 INVALID_PARAMS | ✅ |
| test_call_tool_timeout_returns_tool_timeout_error_code | 超时返回 TOOL_TIMEOUT | ✅ |
| test_gateway_startup_shutdown_with_empty_servers | 空 servers 时 startup/shutdown | ✅ |
| test_call_tool_context_passthrough | context 透传 trace_id | ✅ |

### 2.2 待补充 - MCP API 集成（test_mcp_api.py）

#### TC-MCP-001: GET /mcp/tools 返回工具列表

| 项目 | 内容 |
|------|------|
| **前置条件** | 使用 TestClient(create_app())，app 已注册内部工具 |
| **测试步骤** | 1. client.get("/mcp/tools")<br>2. 校验 status_code=200<br>3. 校验响应为 JSON，tools 为数组，每项含 name、description、inputSchema |
| **预期结果** | 返回至少 5 个内部工具；name 包含 code.read、index.get_status、correlation.get_info、deps.get_graph、analysis.run。注：code.search 仅在 zoekt 配置时存在 |
| **Mock 策略** | 使用最小化 create_app()，zoekt/qdrant 可为 None |
| **优先级** | P1 |

#### TC-MCP-002: POST /mcp/call 执行工具并返回

| 项目 | 内容 |
|------|------|
| **前置条件** | 同 TC-MCP-001；需有 index.get_status 等不依赖 Zoekt 的工具，或 Mock Zoekt |
| **测试步骤** | 1. client.post("/mcp/call", json={"name": "index.get_status", "args": {}})<br>2. 校验 status_code=200<br>3. 校验响应含 content、isError |
| **预期结果** | isError=false 或 true（取决于 index 配置）；content 为数组 |
| **测试数据** | `{"name": "index.get_status", "args": {}}` |
| **优先级** | P1 |

#### TC-MCP-003: POST /mcp/call 工具不存在

| 项目 | 内容 |
|------|------|
| **测试步骤** | client.post("/mcp/call", json={"name": "nonexistent.tool", "args": {}}) |
| **预期结果** | status_code=200，body 中 isError=true，errorCode=TOOL_NOT_FOUND |
| **优先级** | P2 |

#### TC-MCP-004: POST /mcp/call 缺参

| 项目 | 内容 |
|------|------|
| **测试步骤** | 调用需必参的工具（如 analysis.run 需 error_event），args 中缺少必参 |
| **预期结果** | isError=true，errorCode=INVALID_PARAMS，content 含「缺少」等提示 |
| **优先级** | P2 |

---

## 三、AI 网关测试（test_ai_gateway.py）

### TC-AI-001: chat_completion 正常调用

| 项目 | 内容 |
|------|------|
| **前置条件** | AiGateway 初始化，provider 配置有效；Mock LLMProvider.generate 返回固定文本 |
| **测试步骤** | 1. gateway.chat_completion(messages=[{"role":"user","content":"hi"}])<br>2. 校验返回非空 |
| **Mock 策略** | 替换 _providers 中的 LLM 为 AsyncMock，return_value="ok" |
| **优先级** | P1 |

### TC-AI-002: get_provider 返回配置

| 项目 | 内容 |
|------|------|
| **测试步骤** | gateway.get_provider("main") 返回 provider 对象或配置 |
| **预期结果** | 非 None |
| **优先级** | P2 |

### TC-AI-003: add_provider 动态新增

| 项目 | 内容 |
|------|------|
| **测试步骤** | 1. add_provider("extra", AiProviderConfig(...))<br>2. get_provider("extra") 可获取 |
| **预期结果** | 新增后 get_provider 返回非 None |
| **优先级** | P2 |

### TC-AI-004: ENV: 前缀解析

| 项目 | 内容 |
|------|------|
| **测试步骤** | 1. os.environ["TEST_KEY"]="secret123"<br>2. AiProviderConfig(api_key="ENV:TEST_KEY", ...)<br>3. 创建 gateway，调用 chat_completion |
| **预期结果** | 实际请求使用 secret123 作为 api_key |
| **优先级** | P2 |

### TC-AI-005: 指定 provider 不存在时回退 default

| 项目 | 内容 |
|------|------|
| **测试步骤** | chat_completion(..., config_name="nonexistent") |
| **预期结果** | 回退到 default_provider，不抛错 |
| **优先级** | P2 |

### TC-AI-006: create_ai_gateway_from_app_config 兼容 cfg.llm

| 项目 | 内容 |
|------|------|
| **测试步骤** | AppConfig(llm=LlmProviderConfig(...), ai=AiGatewayConfig(providers={}))，providers 为空，llm 有值 |
| **预期结果** | 返回 AiGateway，main provider 来自 llm |
| **优先级** | P2 |

---

## 四、AI 驱动主流程测试（test_orchestrator.py）

### TC-ORC-001: Plan 阶段解析 LLM 返回的 JSON

| 项目 | 内容 |
|------|------|
| **前置条件** | Mock _llm.generate 返回 `{"goal":"...","steps":[{"tool_name":"index.get_status","args":{}}]}` |
| **测试步骤** | orch._plan(event, analysis_id) |
| **预期结果** | 返回 dict，含 steps 列表 |
| **优先级** | P1 |

### TC-ORC-002: Act 阶段按计划调用工具

| 项目 | 内容 |
|------|------|
| **前置条件** | Mock mcp_gateway.call_tool 返回 ToolResult.text("ok") |
| **测试步骤** | orch.analyze(event, analysis_id=...) |
| **预期结果** | call_tool 被调用，参数与 plan 中 steps 一致 |
| **优先级** | P1 |

### TC-ORC-003: 工具失败时抛出 RuntimeError

| 项目 | 内容 |
|------|------|
| **前置条件** | Mock call_tool 返回 ToolResult.error("fail", TOOL_NOT_FOUND) |
| **测试步骤** | orch.analyze(event) |
| **预期结果** | 抛出 RuntimeError，消息含 tool_name 与 error |
| **优先级** | P1 |

### TC-ORC-004: Synthesize 从 tool 结果生成报告

| 项目 | 内容 |
|------|------|
| **前置条件** | plan 中无 analysis.run，仅有 code.search 等；Mock _llm.generate 返回含 summary 的 JSON |
| **测试步骤** | orch.analyze(event) |
| **预期结果** | 返回 AnalysisReport，summary 非空 |
| **优先级** | P2 |

### TC-ORC-005: Check 阶段脱敏

| 项目 | 内容 |
|------|------|
| **测试步骤** | _check_and_sanitize(report_with_ak_sk_in_summary, event) |
| **预期结果** | 返回的 report.summary 中 access_key 等被替换为 [REDACTED:key] |
| **优先级** | P2 |

### TC-ORC-006: Analyzer AI 驱动失败时回退直连

| 项目 | 内容 |
|------|------|
| **前置条件** | ai_driven_enabled=True，Mock orch.analyze 抛出异常 |
| **测试步骤** | analyzer.analyze(event) |
| **预期结果** | 捕获异常后调用 _analyze_direct，最终返回 AnalysisReport |
| **优先级** | P1 |

### TC-ORC-007: ai_driven_enabled=false 走直连

| 项目 | 内容 |
|------|------|
| **前置条件** | ai_driven_enabled=False |
| **测试步骤** | analyzer.analyze(event) |
| **预期结果** | 不调用 AiOrchestrator，直接 _analyze_direct |
| **优先级** | P2 |

### TC-ORC-008: analysis.run 调用时 skip_ai_driven 避免循环

| 项目 | 内容 |
|------|------|
| **测试步骤** | AnalysisRunTool.run(args) 内部调用 analyzer.analyze(..., skip_ai_driven=True) |
| **预期结果** | 不触发 AiOrchestrator，避免 analysis.run -> analyze -> analysis.run 循环 |
| **优先级** | P2 |

---

## 五、脱敏与安全测试

### 5.1 已覆盖（test_utils_redact.py）

| 用例 | 描述 | 状态 |
|------|------|------|
| test_redact_access_key | access_key 脱敏 | ✅ |
| test_redact_password | password 脱敏 | ✅ |
| test_redact_bearer_token | Bearer token 脱敏 | ✅ |
| test_redact_connection_string | 连接串脱敏 | ✅ |
| test_redact_preserves_safe_text | 正常文本保留 | ✅ |
| test_redact_empty | 空输入处理 | ✅ |

### 5.2 待补充

#### TC-REDACT-001: _sanitize_report 保存前脱敏

| 项目 | 内容 |
|------|------|
| **测试步骤** | 构造 AnalysisReport(summary="access_key=sk-xxx")，调用 analyzer._sanitize_report(report) |
| **预期结果** | 返回的 report.summary 不含 sk-xxx，含 [REDACTED:key] |
| **涉及文件** | root_seeker/services/analyzer.py |
| **优先级** | P2 |

#### TC-REDACT-002: Orchestrator Check 阶段脱敏

| 项目 | 内容 |
|------|------|
| **测试步骤** | _check_and_sanitize(report, event)，report 含 hypotheses/suggestions/business_impact 敏感信息 |
| **预期结果** | hypotheses、suggestions、business_impact 均脱敏 |
| **实现** | test_orchestrator.py::test_check_and_sanitize_redacts_all_fields |
| **优先级** | P2 |

---

## 六、配置热更新测试（test_config_reload.py）

#### TC-CFG-001: reload_config_raw 返回新配置

| 项目 | 内容 |
|------|------|
| **前置条件** | 临时写入 config 文件，修改某字段 |
| **测试步骤** | 1. raw1 = reload_config_raw()<br>2. 修改文件<br>3. raw2 = reload_config_raw() |
| **预期结果** | raw2 与 raw1 不同，反映文件变更 |
| **优先级** | P2 |

#### TC-CFG-002: 并发 reload 时 Lock 串行

| 项目 | 内容 |
|------|------|
| **测试步骤** | 多线程同时调用 reload_config_raw，记录执行顺序 |
| **预期结果** | 无竞态，无异常 |
| **优先级** | P3 |

---

## 七、correlation_id 贯通测试（test_correlation_id.py）

#### TC-CID-001: ingest 入队时 event 含 correlation_id

| 项目 | 内容 |
|------|------|
| **测试步骤** | 1. 调用 _enqueue_ingest_event(event)<br>2. 从 job_queue 取 Job（或 Mock enqueue 捕获参数） |
| **预期结果** | Job.event.correlation_id 非空，Job.analysis_id == event.correlation_id |
| **涉及文件** | root_seeker/app.py _enqueue_ingest_event |
| **实现说明** | 由 TC-CID-002/003 及代码审查覆盖：ingest 返回 analysis_id，GET /analysis/{id} 完成报告含 correlation_id |
| **优先级** | P1 |

#### TC-CID-002: AnalysisReport 含 correlation_id

| 项目 | 内容 |
|------|------|
| **测试步骤** | 构造 NormalizedErrorEvent(correlation_id="cid123")，执行 _analyze_direct 或 orch.analyze |
| **预期结果** | 返回的 AnalysisReport.correlation_id == "cid123" |
| **优先级** | P2 |

#### TC-CID-003: GET /analysis/{id} 返回含 correlation_id

| 项目 | 内容 |
|------|------|
| **测试步骤** | 1. ingest 入队并完成分析<br>2. GET /analysis/{analysis_id} |
| **预期结果** | 响应 JSON 含 correlation_id 字段 |
| **优先级** | P3 |

---

## 八、超时/重试测试（test_timeout_retry.py）

#### TC-TIMEOUT-001: Zoekt 超时后重试

| 项目 | 内容 |
|------|------|
| **前置条件** | Mock httpx 客户端，首次请求 sleep 超时，第二次成功 |
| **测试步骤** | ZoektClient.search(query="test") |
| **预期结果** | 最终返回结果，日志含「重试」 |
| **优先级** | P2 |

#### TC-TIMEOUT-002: SLS 查询超时

| 项目 | 内容 |
|------|------|
| **前置条件** | Mock _do_query 阻塞超过 timeout_seconds |
| **测试步骤** | AliyunSlsProvider.query(...) |
| **预期结果** | asyncio.TimeoutError 或 在 wait_for 内抛出 |
| **优先级** | P3 |

---

## 九、外部 MCP Server 测试（test_external_mcp.py）

#### TC-EXT-001: MCP SDK 未安装时 is_mcp_sdk_available=False

| 项目 | 内容 |
|------|------|
| **测试步骤** | 在未安装 mcp 的环境（或 Mock import 失败）下调用 is_mcp_sdk_available() |
| **预期结果** | 返回 False |
| **优先级** | P2 |

#### TC-EXT-002: ENV: 前缀环境变量解析

| 项目 | 内容 |
|------|------|
| **测试步骤** | ExternalMcpSession 初始化 env={"KEY":"ENV:MY_VAR"}，os.environ["MY_VAR"]="val"，connect 时 full_env 含 KEY=val |
| **预期结果** | StdioServerParameters 的 env 中 KEY 为 "val" |
| **优先级** | P2 |

#### TC-EXT-003: 外部工具名带 server 前缀

| 项目 | 内容 |
|------|------|
| **前置条件** | Mock 外部 Server 返回 tools=[{name:"query_logs"}]，server_id="aliyun" |
| **测试步骤** | sess.list_tools() |
| **预期结果** | 返回 ToolSchema(name="aliyun.query_logs", ...) |
| **优先级** | P2 |

---

## 十、向后兼容测试（test_backward_compat.py）

#### TC-BC-001: POST /ingest 返回 analysis_id

| 项目 | 内容 |
|------|------|
| **测试步骤** | client.post("/ingest", json={"service_name":"svc","error_log":"err"}) |
| **预期结果** | status_code=200，body 含 status="accepted"，analysis_id 为 32 位 hex |
| **优先级** | P1 |

#### TC-BC-002: POST /ingest/aliyun-sls 兼容 SLS 格式

| 项目 | 内容 |
|------|------|
| **测试数据** | `{"content":"error msg","__time__":1234567890,"__tag__":{"_container_name":"my-svc"}}` |
| **预期结果** | 解析为 IngestEvent，service_name=my-svc，入队成功 |
| **优先级** | P1 |

#### TC-BC-003: GET /index/status 返回各仓库状态

| 项目 | 内容 |
|------|------|
| **测试步骤** | client.get("/index/status") |
| **预期结果** | status_code=200，JSON 含 repos 或类似结构 |
| **优先级** | P1 |

#### TC-BC-004: GET /analysis/{id} 返回报告或状态

| 项目 | 内容 |
|------|------|
| **测试步骤** | 1. 先 ingest 得到 analysis_id<br>2. client.get(f"/analysis/{analysis_id}") |
| **预期结果** | 返回 status 或 report；pending/running 时含 status 字段，completed 时含 summary |
| **优先级** | P1 |

---

## 十一、测试数据与 Mock 规范

### 11.1 通用 Mock

| 对象 | Mock 方式 | 用途 |
|------|------------|------|
| LLMProvider | AsyncMock，generate 返回固定 JSON | Plan、Synthesize |
| McpGateway | Mock，call_tool 返回 ToolResult | Act 阶段 |
| ZoektClient | Mock 或真实（无 Zoekt 时跳过） | code.search |
| QdrantVectorStore | Mock | index.get_status |
| AliyunSlsProvider | Mock，query 返回 LogBundle | enrich |

### 11.2 最小化 App 配置

```yaml
# 用于集成测试的最小 config
data_dir: 临时目录
aliyun_sls: 必填（可 Mock）
repos: [] 或 1 个本地路径
zoekt: null  # 可选
qdrant: null # 可选
llm: 有 api_key 或 Mock
```

---

## 十二、执行顺序与里程碑

| 阶段 | 内容 | 验收 |
|------|------|------|
| M1 | P1 用例全部实现并通过 | 15+ 用例 |
| M2 | P2 用例 90% 通过 | 25+ 用例 |
| M3 | P3 可选用例、CI 集成 | pytest 纳入 CI |

---

## 十三、验收通过标准

- [x] 所有 P1 用例通过
- [x] 至少 90% P2 用例通过
- [x] `pytest tests/ -v` 无失败
- [ ] 新增 MCP/AI 相关测试纳入 CI
