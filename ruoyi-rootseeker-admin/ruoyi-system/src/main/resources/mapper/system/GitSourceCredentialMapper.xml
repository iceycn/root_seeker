<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.GitSourceCredentialMapper">
    <resultMap type="com.ruoyi.system.domain.GitSourceCredential" id="GitSourceCredentialResult">
        <id property="id" column="id"/>
        <result property="domain" column="domain"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="platform" column="platform"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectById" resultMap="GitSourceCredentialResult">
        select id, domain, username, password, platform, created_at, updated_at
        from git_source_credential where id = #{id}
    </select>

    <insert id="insert" parameterType="com.ruoyi.system.domain.GitSourceCredential">
        insert into git_source_credential (domain, username, password, platform, created_at, updated_at)
        values (#{domain}, #{username}, #{password}, #{platform}, now(), now())
    </insert>

    <update id="update" parameterType="com.ruoyi.system.domain.GitSourceCredential">
        update git_source_credential
        set domain=#{domain}, username=#{username}, password=#{password}, platform=#{platform}, updated_at=now()
        where id = #{id}
    </update>
</mapper>
